apiVersion: v1
kind: Service
metadata:
  name: time-service
  namespace: demo
spec:
  type: NodePort
  selector:
    app: time-service
  ports:
    - port: 9002
      targetPort: 9002
      nodePort: 30002
