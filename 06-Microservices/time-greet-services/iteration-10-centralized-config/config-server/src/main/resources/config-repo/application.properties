# ============================================================
# SHARED CONFIG — Applies to ALL services fetching from config-server
# ============================================================

# Eureka — one place for the registry URL (instead of 3 copies)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
