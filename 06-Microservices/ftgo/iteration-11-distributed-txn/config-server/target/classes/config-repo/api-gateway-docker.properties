# ============================================================
# API GATEWAY DOCKER OVERRIDES
# ============================================================

# CORS — Allow ftgo-web container (Docker internal)
spring.cloud.gateway.globalcors.cors-configurations.[/**].allowed-origins=http://localhost:3000,http://ftgo-web:3000
