# ============================================================
# DOCKER PROFILE — Container hostname overrides
# Activated via SPRING_PROFILES_ACTIVE=docker in docker-compose.yml
# ============================================================
kitchen-service.url=http://kitchen-service:8084
spring.kafka.bootstrap-servers=kafka:9092
