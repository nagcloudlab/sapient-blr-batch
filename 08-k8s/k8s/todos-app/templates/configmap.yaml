apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    {{- include "todos-app.labels" . | nindent 4 }}
data:
  POSTGRES_DB: {{ .Values.postgres.database }}
  SPRING_PROFILES_ACTIVE: {{ .Values.app.springProfile }}
