global:
  scrape_interval: 15s

scrape_configs:
  # Scrape metrics from OTel Collector (which receives from services)
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
