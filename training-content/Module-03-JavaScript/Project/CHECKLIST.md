# Module 03 Checklist -- Participant Submission

## Bug Fixes
- [ ] Bug #1: Adding same item increments quantity (no duplicates)
- [ ] Bug #2: Cart total calculates correctly with and without discount
- [ ] Bug #3: Checkout form validates before submission (no empty fields)
- [ ] Bug #4: Cuisine filter shows/hides cards without page crash

## Enhancements
- [ ] Closed restaurants have disabled buttons
- [ ] Cart badge shows correct total item count
- [ ] Error messages displayed next to invalid form fields

## Debugging Evidence
- [ ] Screenshot of DevTools breakpoint on the NaN bug
- [ ] Brief description of how you traced the bug

## Self-Check Questions
1. What is the difference between `===` and `==`?
2. Why should you use `const` by default instead of `let`?
3. What does `e.preventDefault()` do on a form submit?
4. What is the difference between `find()` and `filter()`?
5. Why is `innerHTML` with user data a security risk?
