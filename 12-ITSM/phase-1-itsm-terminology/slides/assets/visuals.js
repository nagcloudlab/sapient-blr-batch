/* Purpose-built, offline teaching diagrams. No external libraries. */
function renderVisual(s,esc){
 const header=`<p class="eyebrow">${d.id} / ${esc(d.tag)}</p><h2>${esc(s.title)}</h2><p class="lead">${esc(s.lead)}</p>`;
 const label=(a,b)=>`<strong>${esc(a)}</strong><span>${esc(b)}</span>`;
 let v='';
 switch(s.visual){
 case 'stack':v=`<div class="service-stack">${s.items.map(([a,b],i)=>`<div class="stack-tier tier-${i}"><span class="tier-number">0${i+1}</span><div>${label(a,b)}</div></div>${i<2?'<div class="stack-link">depends on ↓</div>':''}`).join('')}<aside><span class="micro">FOLLOW THE VALUE</span><h3>The customer sees<br>“Payment successful.”</h3><p>The provider manages the layers underneath.</p><p class="stack-alert">A fault below can interrupt the outcome above.</p></aside></div>`;break;
 case 'priority':v=`<div class="priority-layout"><table class="priority-grid"><caption>Example priority matrix</caption><thead><tr><th scope="col">Impact ↓ / Urgency →</th><th scope="col">High urgency</th><th scope="col">Low urgency</th></tr></thead><tbody><tr><th scope="row">High impact</th><td class="p1"><b>P1</b>Critical</td><td class="p2"><b>P2</b>High</td></tr><tr><th scope="row">Low impact</th><td class="p3"><b>P3</b>Medium</td><td class="p4"><b>P4</b>Low</td></tr></tbody></table><aside class="visual-aside"><span class="micro">AXIS AT 12:03 PM</span><h3>99.2% → 34%</h3><p>Payments are failing for affected Axis users.</p><p>In this scenario, impact and urgency are both assessed as high.</p><strong class="decision">P1 · Coordinate restoration</strong></aside></div>`;break;
 case 'whys':v=`<ol class="why-chain">${s.items.map(([a,b],i)=>`<li><span class="why-index">${i+1}</span><strong>${esc(a)}</strong><p>${esc(b)}</p></li>`).join('')}</ol><div class="time-ribbon"><span><b>11:45</b> Upgrade</span><span>→</span><span><b>12:03</b> Failure detected</span><span>→</span><span><b>12:22</b> Service restored</span><small>Clock times from the case · RCA works backward</small></div>`;break;
 case 'rollout':v=`<div class="rollout-track">${[10,50,100].map((n,i)=>`<article><span class="micro">STAGE 0${i+1}</span><div class="traffic-number">${n}<small>%</small></div><div class="traffic-grid" aria-label="${n} percent of traffic">${Array.from({length:10},(_,j)=>`<span class="${j<n/10?'filled':''}"></span>`).join('')}</div><h3>${['Observe the canary','Expand after checks','Verify the full rollout'][i]}</h3><p>${['Start small; compare service health with the baseline.','Continue only if agreed stop conditions have not fired.','Example: 99.4% success, no new deployment errors.'][i]}</p></article>`).join('')}</div><div class="time-ribbon"><span><b>90 minutes</b> Example rollout</span><span><b>Sunday 2–4 AM</b> Agreed window</span><span><b>Recovery ready</b> Return to v4.1 if needed</span></div>`;break;
 case 'classify':v=`<div class="classification">${s.items.map(([a,b],i)=>`<article><span class="micro">CASE 0${i+1}</span><h3>${esc(a)}</h3><div class="choices"><button data-choice="incident" data-case="${i}">Incident</button><button data-choice="request" data-case="${i}">Service request</button></div><p class="case-feedback" data-explanation="${esc(b)}" aria-live="polite">Choose a process, then explain why.</p><p class="print-explanation">${esc(b)}</p></article>`).join('')}</div>`;break;
 case 'dependencies':v=`<div class="dependency-layout"><div class="dependency-map"><svg viewBox="0 0 1000 430" aria-hidden="true"><defs><marker id="dep-arrow" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8" fill="#7a9c99"/></marker></defs><g fill="none" stroke="#7a9c99" stroke-width="3" marker-end="url(#dep-arrow)"><path class="impact-edge" d="M500 75 V108 H250 V142"/><path class="impact-edge" d="M500 75 V108 H750 V142"/><path d="M250 215 V260 H95 V302"/><path d="M250 215 V260 H305 V302"/><path class="impact-edge" d="M250 215 V280 H530 V302"/><path class="impact-edge" d="M750 215 V260 H530 V302"/><path d="M750 215 V280 H865 V302"/></g></svg><div class="dep-node affected service-node" style="left:360px;top:0;width:280px">${label('UPI Payments','Business service')}</div><div class="dep-node affected" style="left:110px;top:145px;width:280px">${label('Transaction engine','Payment processing')}</div><div class="dep-node affected" style="left:610px;top:145px;width:280px">${label('Settlement engine','Settlement processing')}</div><div class="dep-node" style="left:0;top:305px;width:190px">${label('Kafka','Event streaming')}</div><div class="dep-node" style="left:210px;top:305px;width:190px">${label('PSP gateway','Bank integration')}</div><div class="dep-node database-node" style="left:410px;top:305px;width:240px">${label('Shared Oracle DB','Used by both apps')}</div><div class="dep-node" style="left:750px;top:305px;width:230px">${label('SFTP server','Settlement files')}</div><span class="edge-label">Arrows point to dependencies ↓</span></div><aside class="visual-aside"><span class="micro">TRY THE SCENARIO</span><h3>What if the shared database fails?</h3><button class="impact-toggle" aria-pressed="false">Show database failure</button><p class="impact-feedback" aria-live="polite">Trace both applications back to the shared database.</p><p class="print-explanation">Database failure can affect both engines and degrade UPI Payments.</p><small>Teaching assumption: both engines depend on this database.</small></aside></div>`;break;
 case 'levels':v=`<div class="level-metrics">${[['SLI','99.94%','What we observe','Measured success rate'],['SLO','99.9%','What we target','Engineering objective'],['SLA','99.5%','What we agree','Customer agreement']].map(([term,n,a,b])=>`<article><span class="micro">${term} · ${a}</span><strong>${n}</strong><p>${b}</p></article>`).join('')}</div><div class="level-track"><div class="level-axis"></div><span class="axis-start">99.0%</span><span class="axis-end">100%</span><div class="level-marker sla" style="left:50%"><b>99.5%</b><span>SLA</span></div><div class="level-marker slo" style="left:90%"><b>99.9%</b><span>SLO</span></div><div class="level-marker sli" style="left:94%"><b>99.94%</b><span>SLI</span></div></div><p class="visual-caption">Zoomed scale: 99–100% success · same metric and reporting window</p>`;break;
 case 'downtime':{const mins=[432,216,43.2,21.6,4.32];v=`<div class="downtime-chart"><div class="chart-axis">0 <span>432 minutes / 7 h 12 min</span></div>${s.items.map(([a,b],i)=>`<div class="downtime-row"><strong>${esc(a)}</strong><div class="downtime-rail"><span style="width:${mins[i]/432*100}%"></span></div><p>${esc(b.replace(' of downtime.','.'))}</p></div>`).join('')}<p class="visual-caption">Linear scale · fixed 30-day month · shorter bar means less allowed downtime</p></div>`;break;}
 case 'budget':v=`<div class="budget-layout"><div><span class="micro">ONE MILLION ELIGIBLE REQUESTS · 99.9% SLO</span><h3>1,000 failures available in the budget</h3><div class="budget-grid" role="img" aria-label="100 blocks: 60 used and 40 remaining. Each block represents 10 failures.">${Array.from({length:100},(_,i)=>`<span class="${i<60?'spent':'available'}"></span>`).join('')}</div><p class="visual-caption">Each square = 10 failures · this grid shows the error budget, not all requests</p></div><aside class="budget-summary"><div><span class="micro">USED · 60%</span><strong>600</strong></div><div><span class="micro">REMAINING · 40%</span><strong>400</strong></div><p>1,000,000 × 0.1% = 1,000<br>1,000 − 600 = 400</p></aside></div>`;break;
 case 'cycle':v=`<div class="pdca-loop"><svg viewBox="0 0 1428 450" aria-hidden="true"><defs><marker id="cycle-arrow" markerWidth="9" markerHeight="9" refX="7" refY="4.5" orient="auto"><path d="M0,0 L9,4.5 L0,9" fill="#177c82"/></marker></defs><g fill="none" stroke="#177c82" stroke-width="4" marker-end="url(#cycle-arrow)"><path d="M600 88 H818"/><path d="M1120 175 V263"/><path d="M828 360 H610"/><path d="M300 275 V185"/></g></svg>${s.items.map(([a,b],i)=>`<article class="cycle-step step-${i}"><span class="cycle-num">0${i+1}</span><div><h3>${esc(a)}</h3><p>${esc(b)}</p></div></article>`).join('')}<div class="cycle-center">Learn<br>and repeat</div></div>`;break;
 }
 return header+`<div class="visual-body visual-${s.visual}">${v}</div><p class="takeaway"><span>Remember</span>${esc(s.takeaway)}</p>`;
}
function initVisuals(){
 document.querySelectorAll('[data-choice]').forEach(button=>button.addEventListener('click',()=>{
  const box=button.closest('article'),correct=Number(button.dataset.case)===0?'incident':'request',ok=button.dataset.choice===correct;
  box.querySelectorAll('[data-choice]').forEach(b=>{b.classList.toggle('selected',b===button);b.setAttribute('aria-pressed',String(b===button))});
  const feedback=box.querySelector('.case-feedback');feedback.classList.toggle('correct',ok);feedback.classList.toggle('retry',!ok);feedback.textContent=(ok?'Correct. ':'Reconsider the trigger. ')+feedback.dataset.explanation;
 }));
 document.querySelectorAll('.impact-toggle').forEach(button=>button.addEventListener('click',()=>{
  const root=button.closest('.dependency-layout'),active=root.classList.toggle('failure');button.setAttribute('aria-pressed',String(active));button.textContent=active?'Reset to healthy state':'Show database failure';
  root.querySelector('.database-node span').textContent=active?'FAILED · unavailable':'Used by both apps';
  root.querySelector('.impact-feedback').textContent=active?'Both engines depend on the failed database. UPI Payments can be degraded.':'Trace both applications back to the shared database.';
 }));
}
