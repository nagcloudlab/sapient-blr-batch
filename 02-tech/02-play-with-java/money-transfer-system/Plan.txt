

-----------------------
money-transfer-system
-----------------------

    database -> SQL database -> MySQL
    console-application -> Java


database
---------

    relations(s)

    1. accounts
    2. transfer_history


   create database money_transfer_system_db;
   use money_transfer_system_db;
   create table accounts (
       account_number VARCHAR(20) NOT NULL UNIQUE,
       account_holder_name VARCHAR(100) NOT NULL,
       balance DECIMAL(15, 2) NOT NULL
   ); 
   create table transfer_history (
       id INT AUTO_INCREMENT PRIMARY KEY,
       from_account VARCHAR(20) NOT NULL,
       to_account VARCHAR(20) NOT NULL,
       amount DECIMAL(15, 2) NOT NULL,
       transfer_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
   );

   insert into accounts (account_number, account_holder_name, balance) values
   ('ACC123', 'John Doe', 1000.00),
   ('ACC456', 'Jane Smith', 500.00)


application
------------

step-1: define data model(s)

    1. Account
        - accountNumber: String
        - accountHolderName: String
        - balance: BigDecimal

    2. TransferHistory
        - id: int
        - fromAccount: String
        - toAccount: String
        - amount: BigDecimal
        - transferDate: Timestamp

step-2: define data-access / repository layer(s)

    1. AccountRepository
        - getAccountByNumber(String accountNumber): Account
        - updateAccount(Account account): void

    2. TransferHistoryRepository
        - 
        - 